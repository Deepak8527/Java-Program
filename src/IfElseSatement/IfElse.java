package IfElseSatement;

public class IfElse {

	public static void main(String[]args) {
		
		boolean IsMale = false;
		// we are use in if-else statement which are the condition based statements
		String name = "tom";
		if (IsMale) {
		
			System.out.println("You are a male " + name);
		}
		 else{
			
	    	System.out.println("You are not a male " + name);
			}
		}
	}

